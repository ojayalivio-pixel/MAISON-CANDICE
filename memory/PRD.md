# PRD — Candice Private Portfolio

## Original Problem Statement
Professional, sophisticated yet explicit adult portfolio website. Requirements:
- Premium aesthetics (dark theme, premium typography, micro-animations)
- 18+ age gate
- "Easy to paste" portable — pure static HTML/CSS/JS, no backend
- Hidden admin mode (URL hash `#backstage` or Ctrl/Cmd+Shift+K), invisible to visitors
- Inline WYSIWYG editing of text + swap photos/videos from admin
- IP-based country blocking (ipapi.co, no key required)
- Admin code separated from index.html (admin.css / admin.js), with an Export button that bakes everything into one self-contained index.html

## Architecture
```
/app/index.html   — public site (references admin.css/admin.js)
/app/admin.css    — admin panel styles
/app/admin.js     — admin logic: login, WYSIWYG, media picker, geo-block, export
/app/frontend/    — minimal node static server shim (server.js) so supervisor serves /app on port 3000 (preview only, NOT part of the exported product)
```
State: localStorage keys `candice_admin_pass`, `candice_content`, `candice_media`, `candice_blocked_countries`; session key `candice_admin_session`.

## Implemented (as of June 2026)
- Full premium redesign + age gate — DONE
- Hidden admin login (password `candice2026`, changeable in Settings) — DONE
- WYSIWYG text editing with localStorage persistence — DONE
- Media picker: upload (data URL, ~4MB cap) or paste URL for gallery tiles + video slot — DONE
- Country blocking via ipapi.co with admin exemption — DONE
- Export as single self-contained index.html — DONE
- Static server shim for preview (2026-06) — DONE

## Backlog
- P2: None pending — awaiting user feedback

## Change log
- 2026-06: SECURITY AUDIT + fixes (iteration_11.json — 100% PASS, 26/26). Audit found: SEC-001 CRITICAL (admin password 'candice2026' hardcoded in public admin.js AND matches backend ADMIN_UPLOAD_PASS → anyone can call admin APIs) — STILL OPEN, needs server-side auth redesign (auth=integration, pending user consent). SEC-002 CRITICAL (static server ROOT='/app' served backend/.env + source + /memory → secret leak; confirmed /backend/.env returned 200 with MONGO_URL/EMERGENT_LLM_KEY/ADMIN_UPLOAD_PASS) — FIXED: /app/frontend/server.js now uses explicit ALLOWLIST (index.html, admin.js, admin.css, favicons, og-image.jpg), basename-flatten + index.html fallback; secrets no longer served. SEC-003 HIGH (uploads accepted any content_type, served w/o nosniff → stored XSS) — FIXED: server.py upload_complete rejects non image/video (415); serve_file adds X-Content-Type-Options:nosniff + CSP sandbox. Remaining hardening (P3): CORS='*', no rate limiting, plaintext non-constant-time password compare, client-enforced geo-block (cosmetic), XFF-trusted /api/geo. NOTE: .env was publicly exposed before SEC-002 fix → admin password + EMERGENT_LLM_KEY should be rotated. Security regression suite: /app/backend/tests/test_security_fixes.py.
- 2026-06: CRITICAL geo-block fix — real visitors weren't getting blurred (iteration_9.json + iteration_10.json, both 100% PASS). TWO root causes: (A) blocklist was in per-device localStorage so real visitors' browsers had an empty list → moved to BACKEND: new GET /api/blocked-countries (public) + POST /api/blocked-countries (X-Admin-Pass protected), stored in db.settings key 'blocked_countries'. admin.js toggleBlocked() POSTs to server; syncBlockedFromServer() runs on openAdmin()+switchTab('geo'); enforceGeoBlock() fetches list from server (localStorage only as offline fallback). (B) country detection used ipapi.co directly from browser = CORS-blocked ('Failed to fetch') → added NEW backend GET /api/geo (reads X-Forwarded-For, server-side ip-api.com lookup, returns {country_code,country}); both admin.js enforceGeoBlock and index.html visit-ping now call /api/geo. Verified no-mock e2e: browser detected US → blocked US on server → fresh visitor (null localStorage) got blur(12px)+banner. NOTE: whole geo feature now requires the backend (won't work on the free one-file export / backend-less hosting). ip-api.com free tier = 45 req/min/IP, failure-open.
- 2026-06: Region preview + stronger blur (self-tested via screenshot w/ computed-style asserts). (1) Added Backstage COUNTRIES-tab button #btnPreviewGeo [data-testid=preview-blocked-btn] "👁 Preview as blocked country" — toggles body.geo-blur on the admin's own view (label flips to "✕ Exit preview"); fully reversible (only toggles the class, does not strip options). Updated COUNTRIES tip text to describe blur behavior. (2) Blur strength increased 7px→12px in admin.css body.geo-blur [data-restrict]. (3) LIVE stats error clarified for user = deployment issue: backend not reachable on maisoncandice.com; needs full Emergent Deploy (not one-file export) + Link domain. No code fix possible from here for that.
- 2026-06: COUNTRY-PRIVACY changed HIDE→BLUR + STATS bug fix (iteration_8.json — 100% PASS, 29/29). (1) Blocked-country visitors now SEE the sensitive escort/in-person parts BLURRED (filter:blur(7px), visible-but-unreadable) instead of hidden; non-explicit text stays fully readable and TRAVEL nav link stays visible/crisp. data-restrict now on: #travel .location, .availability, .travel-list, 'Meet me somewhere' btn; the 'Virtual Dates & In Person' card; booking in-person toggle field. Removed the section-replace privacy panel + restrict-hide/restrict-msg. Banner: 'Some content is blurred for privacy in your region'. restrictSensitive() still removes in-person <option>s. (2) BUG FIX 'Couldn't load stats — backend unreachable': root cause = changePassword() saved new password to localStorage even when backend didn't confirm → browser password desynced from ADMIN_UPLOAD_PASS → all /api calls 401 → surfaced as 'unreachable'. Fix: changePassword only persists on backend 200; on network error shows 'Backend unreachable — password unchanged' (no local save). loadStats now shows a clear 'password doesn't match the server' message on 401 vs 'unreachable' on true network error. Backend candice2026 unchanged.
- 2026-06: COUNTRY-PRIVACY feature (iteration_7.json — 100% PASS, selective). Replaced prior "blur all words" behavior. When admin blocks a country (Backstage → COUNTRIES tab, localStorage 'candice_blocked_countries'), a visitor from that country has ONLY escort/in-person tells restricted; everything else stays normal. Tagged with data-restrict: (a) TRAVEL section (id=travel, data-restrict="section") — real content .restrict-hide hidden, .restrict-msg privacy panel "Kept private in your region." shown; (b) "Virtual Dates & In Person" experience card; (c) booking "Virtual or in person" toggle field; (d) TRAVEL nav link. admin.js restrictSensitive() adds body.geo-blur + removes any <option> containing "in person". Discreet fixed bottom banner "Some details are kept private in your region". Admin (isLoggedIn) exempt; empty blocklist = no restriction. Exposed window.enforceGeoBlock/window.restrictSensitive. Section ids: about=#candice, pay=#payments, connect=#contact.
- 2026-06: Removed CONNECT red border (transparent border+bg). Yellow-line reported but NOT reproducible (0 flush-left elements, desktop+mobile).
- 2026-06: Three fixes (iteration_6.json — 100% PASS). (1) BUG FIX: floating "SAVED" indicator at bottom-left visible to visitors — root cause: admin.js injected #adminToast with literal default text "Saved" for every visitor (position:fixed bottom-left). Fix: emptied default text (`<div class="admin-toast" id="adminToast"></div>`) + made .admin-toast opacity:0/visibility:hidden by default, only shown via .show class. Toast still works transiently inside backstage. (2) FAVICON: new gold "C" monogram — /app/favicon.svg (primary, image/svg+xml) + favicon.png rasterized to 3KB (was 597KB) + apple-touch-icon.png; head updated. Removed stray favicon.pvvng. (3) FAST/SECURE: favicon 597KB→3KB; server.js now sends X-Content-Type-Options:nosniff, Referrer-Policy, Permissions-Policy, Cache-Control (immutable for static assets, short for html). No frame-blocking headers (preview iframe safe).
- 2026-06: Social sharing preview fix (iteration_5.json — 100% PASS). Root cause: site had ZERO Open Graph/Twitter tags → Messenger spinner. Added full OG set (og:type/site_name/url/title/description/image + image:secure_url/type/width=1200/height=630/alt/locale) + Twitter card (summary_large_image, title, description, image) to index.html <head>. Created optimized branded preview /app/og-image.jpg (1200x630, ~48KB, on-brand dark/gold "CANDICE FERRAGAMO" card — no explicit imagery so crawlers don't reject it). URLs point to https://maisoncandice.com/og-image.jpg?v=1. NOTE for user: after deploy, run FB Sharing Debugger (developers.facebook.com/tools/debug) on production URL to force Messenger to re-scrape. No design/content change.
- 2026-06: (1) Booking form gained Virtual/In Person segmented toggle (data-testids booking-mode-virtual/inperson), sent as `mode`, stored by backend, shown in REQUESTS cards; (2) Nav rebuilt: ABOUT · EXPERIENCES · GALLERY · TRAVEL · BOOK · PAY · FAQ · CONNECT with gold dot separators (.nav-dot), PAY → #payments. Verified via curl + browser
- 2026-06: Booking system (iteration_4.json — 13/13 backend + all frontend PASS): #booking section "Request a session" form (name/channel/handle/experience/preferred/message, data-testids booking-*), POST /api/bookings + GET (X-Admin-Pass) + status update + delete; Backstage REQUESTS tab with unread highlighting, mark-handled, delete, XSS-escaped rendering (esc()). Nav BOOK link added. Travel dates got month labels (JUL 04 — 13 etc.). Regression suite: /app/backend/tests/test_bookings.py
- 2026-06: (1) OnlyFans removed everywhere (hero pill + contact card + admin tip); (2) Hero title-cased "Come Play in / My Private World, / Candice Ferragamo."; (3) "Virtual Dates" → "Virtual Dates & In Person" with updated copy; menu reordered: Live, Private, Virtual/In-Person, BDSM, Findom, Slave Training, then Roleplay/Voice/Tailored; (4) Country Peek — visit ping now sends ipapi country, stats endpoint returns top-10 countries, STATS tab renders name+bar+count. Verified via curl + browser; test visits cleared
- 2026-06: (1) Age gate fully removed (markup + script + body.locked; CSS kept as .age-eyebrow reused by geo-block); (2) "Gifts & Support" renamed "Gifts & Private Arrangements"; (3) Real Telegram set: t.me/privatemaisoncandice (hero + contact card); (4) Visit counter — POST /api/visits (dedupe per vid/day, admin sessions skipped), GET /api/visits/stats (X-Admin-Pass), STATS tab in Backstage (today/7d/all-time). Verified via curl + browser; test visits cleared
- 2026-06: Privacy note rewritten in first person (user-provided text) — screenshot verified
- 2026-06: Three features (iteration_3.json 10/10 PASS): (1) Signature loading veil — red silk curtain + gold C monogram, splits at 1.35s, removed at 2.6s; (2) Social buttons — hero social strip (OnlyFans/Telegram/WhatsApp/Snapchat pills, data-testids hero-*-btn) + OnlyFans & activated Telegram contact cards (PLACEHOLDER handles: candiceferragamo); (3) Editable link hrefs — edit mode click on .pay-btn/.contact-btn/.hs-btn prompts for URL, stored in localStorage candice_links, applied via loadLinks(), included in export boot
- 2026-06: Typography swapped to old-money pairing — Cormorant Garamond (serif display) + Jost (Futura-style sans) across index.html and admin.css; pink accent (--rose #f4c7d4) changed to red (#ff4d5e). Screenshot verified
- 2026-06: "Send Tribute" shiny gold button added to Findom card → anchors to #payments (screenshot verified)
- 2026-06: Password change now syncs to backend (POST /api/admin/password, stored in Mongo settings; check_pass reads DB first, env fallback). curl verified: change → old pass 401, new pass 200, wrong current 401. Default remains candice2026 until user changes it in Settings
- 2026-06: Cloud media storage added — FastAPI backend (/app/backend/server.py) with chunked upload (init/chunk/complete) → Emergent Object Storage, served via /api/media/file/{id}. Admin uploads (photos 15MB, videos 200MB) now cloud-backed, X-Admin-Pass header auth (env ADMIN_UPLOAD_PASS=candice2026). Tested: iteration_2.json 6/6 PASS
- 2026-06: About section rewritten classy/elegant ("Grace, poise & quiet power."), removed "maldita" line; tags updated (Elegant, Commanding)
- 2026-06: Added experience cards: 07 BDSM & Domination, 08 Findom, 09 Slave Training
- 2026-06: Full admin flow verified via testing agent (13/13 PASS, /app/test_reports/iteration_1.json)
- 2026-06: Hero accent line changed from "darling." to "candice ferragamo."
